#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

if command -v pnpm >/dev/null 2>&1; then
  RUNNER="pnpm"
else
  RUNNER="npm run"
fi

echo "[pre-commit] lint + security scan"
$RUNNER lint
$RUNNER security:scan
